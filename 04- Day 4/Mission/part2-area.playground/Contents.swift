import UIKit

// Area
let triangleLegth = 5
let triangleWidth = 4

// a^2 + b^2 = c^2
let areaTriangle = sqrt(pow(Double(triangleLegth), 2) + pow(Double(triangleWidth), 2))
let areaTriangleStr = String(format: "%.2f", areaTriangle)
print(areaTriangleStr)













